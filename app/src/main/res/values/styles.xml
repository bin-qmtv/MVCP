<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- AwesomeDialog -->
    <style name="AwesomeDialogTheme" parent="ThemeOverlay.AppCompat.Dialog.Alert">
        <item name="android:windowAnimationStyle">@style/AwesomeDialogAnimation</item>
    </style>

    <style name="AwesomeDialogTheme.Transparent">
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

    <style name="AwesomeBottomSheetDialog" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="android:colorBackground">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@style/AwesomeBottomSheetDialogAnimation</item>
    </style>

    <style name="AwesomeDialogAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit</item>
    </style>

    <style name="AwesomeBottomSheetDialogAnimation" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/bottom_sheet_dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/bottom_sheet_dialog_exit</item>
    </style>

    <integer name="AwesomeDialogDuration">160</integer>
    <integer name="AwesomeBottomSheetDialogDuration">260</integer>
    <!-- AwesomeDialog end-->

    <declare-styleable name="StateView">
        <attr name="state_loadingView" format="reference" />
        <attr name="state_emptyView" format="reference" />
        <attr name="state_errorView" format="reference" />
        <attr name="state_offlineView" format="reference" />
        <attr name="state_viewState" format="enum">
            <enum name="none" value="0" />
            <enum name="loading" value="1" />
            <enum name="empty" value="2" />
            <enum name="error" value="3" />
            <enum name="offline" value="4" />
        </attr>
        <attr name="state_animateViewChanges" format="boolean" />
    </declare-styleable>
</resources>
